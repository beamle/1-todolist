import * as appSelectors from "./app.selectors";

// same folder documents can access files without importing files from index.
// but all outer folders have to use interface we provide in index file.

export {
    appSelectors
}

